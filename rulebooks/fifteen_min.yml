---
- name: activation_fail_before_hello
  hosts: all
  sources:
    - name: fail_at_startup
      eda.builtin.range:
        limit: not_a_number
  rules:
    - name: Hello must never run
      condition: event.i == 0
      action:
        run_job_template:
          name: Hello
          organization: Default
